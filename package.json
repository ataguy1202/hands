{
  "name": "hands",
  "version": "0.1.0",
  "private": true,
  "description": "Discover a UI flow once with an LLM, save it as a typed capability, replay it deterministically.",
  "type": "module",
  "engines": {
    "node": ">=22.9"
  },
  "scripts": {
    "target": "tsx target/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.126.0",
    "playwright": "^1.63.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^5.0.1"
  }
}
