# Model access for discovery runs only. Replay never uses it.
ANTHROPIC_API_KEY=

# Credentials the runtime fills into the target's login form.
# They are referenced by name from artifacts ({"secret": "MERIDIAN_PASSWORD"}) and never written to disk.
MERIDIAN_USER=operator
MERIDIAN_PASSWORD=meridian1
